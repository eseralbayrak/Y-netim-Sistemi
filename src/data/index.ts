import type { Material } from "../types";

// Malzemeler ve tedarikçiler sunucu (server/data) tarafından yönetilir.
export const seedMaterials: Material[] = [];
export const seedSuppliers: string[] = [];

